apiVersion: certificates.k8s.io/v1beta1
kind: CertificateSigningRequest
metadata:
    name: user-name
spec:
    groups:
    - system:authenticated
    usages:
    - digital signature
    - key encipherment
    - server auth
    request:
        rtyuiyfdfghjgfgn (This is the encoded text generated by `cat user-name.csr | base64 | tr -d "\n"`)
    signerName: kubernetes.io/kube-apiserver-client